name: SeventySixCommerce TanStack CI

on:
  push:
    branches: [main]
    paths: ['seventysixcommerce-tanstack/**']
  pull_request:
    branches: [main]
    paths: ['seventysixcommerce-tanstack/**']

defaults:
  run:
    working-directory: seventysixcommerce-tanstack

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:17-alpine
        env:
          POSTGRES_USER: SeventySixCommerce
          POSTGRES_PASSWORD: seventysixcommerce_ci
          POSTGRES_DB: SeventySixCommerce
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U SeventySixCommerce"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 5

    env:
      DATABASE_URL: postgresql://SeventySixCommerce:seventysixcommerce_ci@localhost:5432/SeventySixCommerce

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: seventysixcommerce-tanstack/package-lock.json

      - run: npm ci
      - run: npx tsc --noEmit
      - run: npx vitest run
      - run: npx vinxi build
