/**
 * Account-Admin Integration barrel export.
 *
 * This integration handles cross-domain features between Account and Admin:
 * - Permission request types used by both domains
 *
 * Domains import from here instead of @shared/api for these specific types.
 */

export * from "./generated-open-api";
