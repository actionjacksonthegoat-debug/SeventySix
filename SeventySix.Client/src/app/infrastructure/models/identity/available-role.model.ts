/**
 * Role available for permission requests.
 * Shared between account and admin features.
 * Maps to server AvailableRoleDto.
 */
export interface AvailableRole
{
	name: string;
	description: string;
}
