# SeventySix

VS Code should auto format the client code, this can be opened at the top folder level or at the client folder level.
VS 2022+ should auto format, the server solution must be opened to autoformat and see warnings
